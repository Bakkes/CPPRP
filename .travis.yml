language: cpp
dist: bionic
compiler:
  - gcc
  - clang
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-9
    - clang-8
install:
- "[ $CXX = g++ ] && export CXX=g++-9 || true"
- "[ $CXX = clang++ ] && export CXX=clang++-8 || true"
script:
  - make RELEASE=1
